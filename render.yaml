services:
  - type: web
    name: h2o-api
    env: python
    pythonVersion: 3.10
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: python api.py
    plan: free
